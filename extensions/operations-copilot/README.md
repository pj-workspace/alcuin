# Operations Copilot Extension

An explicit domain example for Alcuin. It contributes an Agent Definition, manifest,
declarative UI blocks, and a trusted in-process demo adapter.

The package depends only on `alcuin-core`; it is never imported or installed by the Core API.
