use_default_settings: true

server:
  secret_key: "alcuin-local-searxng-change-before-production"
  bind_address: "0.0.0.0"
  limiter: false
  base_url: false

search:
  formats:
    - html
    - json

outgoing:
  request_timeout: 8.0

engines:
  - name: baidu
    disabled: false
